# Reference data

- `Mumbai Local Train Dataset.csv` — user-supplied station metadata for all Mumbai suburban lines (distances, inter-station runtimes, platforms, tracks). Used to calibrate sim kinematics; also holds platform counts (station dressing) and station lists for future lines. Third-party dataset, license unverified — reference only, not redistributed as part of the app bundle.
